[package]
name = "rwa-risk-registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
odra = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
